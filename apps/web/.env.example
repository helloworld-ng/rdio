WEB_PORT=5173
# URL of the running API. Production web builds should usually use /api.
VITE_API_BASE_URL=http://localhost:3001/api
# Target for requests made through the Vite dev-server /api proxy.
RDIO_API_PROXY_TARGET=http://localhost:3001
