WEB_PORT=5173
# URL of the running API.
VITE_API_BASE_URL=http://localhost:3001
# Must match the API app's API_KEY. Leave blank when API auth is disabled locally.
VITE_API_KEY=
# Target for requests made through the Vite dev-server /api proxy.
RDIO_API_PROXY_TARGET=http://localhost:3001
