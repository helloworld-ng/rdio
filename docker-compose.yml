services:
  icecast:
    image: moul/icecast
    environment:
      ICECAST_SOURCE_PASSWORD: ${ICECAST_SOURCE_PASSWORD:-sourcepass}
      ICECAST_ADMIN_PASSWORD: ${ICECAST_ADMIN_PASSWORD:-adminpass}
      ICECAST_RELAY_PASSWORD: ${ICECAST_RELAY_PASSWORD:-relaypass}
      ICECAST_HOSTNAME: ${ICECAST_HOSTNAME:-localhost}
    ports:
      - "8000:8000"

  liquidsoap:
    image: savonet/liquidsoap:v2.2.5
    depends_on:
      - icecast
    environment:
      ICECAST_HOST: icecast
      ICECAST_PORT: "8000"
      HARBOR_PORT: "8005"
      ICECAST_SOURCE_PASSWORD: ${ICECAST_SOURCE_PASSWORD:-sourcepass}
    volumes:
      - ./services/liquidsoap:/rdio:ro
      - ./media:/media
    ports:
      - "8005:8005"
    command: liquidsoap /rdio/station.liq

# Postgres is defined for when packages/db is implemented.
# Uncomment to start the database alongside the radio services.
#
#  postgres:
#    image: postgres:16-alpine
#    environment:
#      POSTGRES_DB: rdio
#      POSTGRES_USER: rdio
#      POSTGRES_PASSWORD: rdio
#    ports:
#      - "5432:5432"
#    volumes:
#      - postgres_data:/var/lib/postgresql/data

#volumes:
#  postgres_data:
